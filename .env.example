# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Google Maps JavaScript API key. Restrict it by HTTP referrer to the Pages
# domain (*.pages.dev) in the Google Cloud console. Enable "Maps JavaScript API"
# and "Places API". Left blank, maps render a "needs API key" note instead.
PUBLIC_GOOGLE_MAPS_API_KEY=

# Base URL of the deployed hearts Worker, e.g.
#   https://july-trip-hearts.<your-account>.workers.dev
# Left blank, hearts work locally per device but counts are not shared.
PUBLIC_HEART_API_URL=
